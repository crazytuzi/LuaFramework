local ccs = ccs or {}
ccs.MovementEventType = {
    START = 0,
    COMPLETE = 1,
    LOOP_COMPLETE = 2,
}

BattleMgr = {}

function BattleMgr.getCardPosByIndex(atkCard,beAtkCards,index)
	--通过index获取对应的坐标点
	
end

return BattleMgr