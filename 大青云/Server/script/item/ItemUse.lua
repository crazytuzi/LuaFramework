
function ItemAddHP(user,item,context)
	local ret = _ItemAddHP(user,item,context)
	return ret or -1
end

function ItemAddMP(user,item,context)
	local ret = _ItemAddMP(user,item,context)
	return ret or -1
end

function ItemAddHPMP(user,item,context)
	-- TODO:
	return 0 or -1
end

function ItemAddCash(user,item,context)
	-- TODO:
	return 0 or -1
end

function ItemAddExp(user,item,context)
	 local ret = _ItemAddExp(user,item,context)
	 return ret or -1
end

function ItemTeleport(user,item,context)
	-- TODO:
	return 0 or -1
end

function ItemQuest(user,item,context)
     local ret = _ItemQuest(user,item,context)
	 return ret or -1
end

function ItemPackageItem(user, item, context)
     local ret = _ItemPackageItem(user,item,context)
	 return ret or -1
end

function ItemPackageItemCost(user, item, context)
     local ret = _ItemPackageItemCost(user,item,context)
	 return ret or -1
end

function ItemHorn(user, item, context)
     local ret = _ItemHorn(user,item,context)
	 return ret or -1
end

function ItemYaoDan(user, item, context)
	local ret = _ItemYaoDan(user,item,context)
	return ret or -1
end

function ItemSkillProcess(user, item, context)
	local ret = _ItemSkillProcess(user,item,context)
	return ret or -1
end

function ItemAddStat(user, item, context)
	local ret = _ItemAddStat(user,item,context)
	return ret or -1
end

function ItemAddBuff(user, item, context)
	local ret = _ItemAddBuff(user,item,context)
	return ret or -1
end	

function ItemTeleport(user, item, context)
	local ret = _ItemTeleport(user,item,context)
	return ret or -1
end

function ItemAddFashion(user, item, context)
	local ret = _ItemAddFashion(user,item,context)
	return ret or -1
end

function ItemAddProficiency(user, item, context)
	local ret = _ItemAddProficiency(user,item,context)
	return ret or -1
end

function ItemAddLingQiProficiency(user, item, context)
	local ret = _ItemAddLingQiProficiency(user,item,context)
	return ret or -1
end

function ItemAddBaoJiaProficiency(user, item, context)
	local ret = _ItemAddBaoJiaProficiency(user,item,context)
	return ret or -1
end

function ItemAddMingYuProficiency(user, item, context)
	local ret = _ItemAddMingYuProficiency(user,item,context)
	return ret or -1
end

function ItemCreateSuper(user, item, context)
	local ret = _ItemCreateSuper(user,item,context)
	return ret or -1
end

function ItemAddSuper(user, item, context)
	local ret = _ItemAddSuper(user,item,context)
	return ret or -1
end

function ItemAddRandomSuper(user, item, context)
	local ret = _ItemAddRandomSuper(user,item,context)
	return ret or -1
end

function ItemAddEnergy(user, item, context)
	local ret = _ItemAddEnergy(user,item,context)
	return ret or -1
end

function ItemAddVitality(user, item, context)
	local ret = _ItemAddVitality(user,item,context)
	return ret or -1
end

function ItemAddCashGift(user, item, context)
	local ret = _ItemAddCashGift(user,item,context)
	return ret or -1
end

function ItemAddHorseSkin(user, item, context)
	local ret = _ItemAddHorseSkin(user,item,context)
	return ret or -1
end

function ItemAddHorseAttr(user, item, context)
	local ret = _ItemAddHorseAttr(user,item,context)
	return ret or -1
end

function ItemAddBingHun(user, item, context)
	local ret = _ItemAddBingHun(user,item,context)
	return ret or -1
end

function ItemAddTitle(user, item, context)
	local ret = _ItemAddTitle(user,item,context)
	return ret or -1
end

function ItemAddHunZhi(user, item, context)
	local ret = _ItemAddHunZhi(user,item,context)
	return ret or -1
end

function ItemVipType(user, item, context)
	local ret = _ItemVipType(user,item,context)
	return ret or -1
end
function ItemAddRedPacket(user, item, context)
	local ret = _ItemAddRedPacket(user,item,context)
	return ret or -1
end

function ItemAddShenBing(user, item, context)
	local ret = _ItemAddShenBing(user,item,context)
	return ret or -1
end

function ItemAddBaoJia(user, item, context)
	local ret = _ItemAddBaoJia(user,item,context)
	return ret or -1
end

function ItemAddMingYu(user, item, context)
	local ret = _ItemAddMingYu(user,item,context)
	return ret or -1
end

function ItemAddBaoJiaWish(user, item, context)
	local ret = _ItemAddBaoJiaWish(user,item,context)
	return ret or -1
end



function ItemAddShenBingAttr(user, item, context)
	local ret = _ItemAddShenBingAttr(user,item,context)
	return ret or -1
end

function ItemAddLingQi(user, item, context)
	local ret = _ItemAddLingQi(user,item,context)
	return ret or -1
end

function ItemAddLingQiAttr(user, item, context)
	local ret = _ItemAddLingQiAttr(user,item,context)
	return ret or -1
end

function ItemAddLingZhenAttr(user, item, context)
	local ret = _ItemAddLingZhenAttr(user,item,context)
	return ret or -1
end

function ItemAddZhanYin(user, item, context)
	local ret = _ItemAddZhanYin(user,item,context)
	return ret or -1
end

function ItemSuperExp(user, item, context)
	local ret = _ItemSuperExp(user,item,context)
	return ret or -1
end

function ItemAddJiaoYin(user, item, context)
	local ret = _ItemAddJiaoYin(user,item,context)
	return ret or -1
end

function ItemAddZhanYinQuality(user, item, context)
	local ret = _ItemAddZhanYinQuality(user,item,context)
	return ret or -1
end

function ItemAddZhanYinType(user, item, context)
	local ret = _ItemAddZhanYinType(user,item,context)
	return ret or -1
end

function ItemDailyPackage(user, item, context)
	local ret = _ItemDailyPackage(user,item,context)
	return ret or -1
end

function ItemAddZhanYinChip(user, item, context)
	local ret = _ItemAddZhanYinChip(user,item,context)
	return ret or -1
end

function ItemAddShenBingWish(user, item, context)
	local ret = _ItemAddShenBingWish(user,item,context)
	return ret or -1
end

function ItemAddLingQiWish(user, item, context)
	local ret = _ItemAddLingQiWish(user,item,context)
	return ret or -1
end

function ItemAddLingZhenWish(user, item, context)
	local ret = _ItemAddLingZhenWish(user,item,context)
	return ret or -1
end

function ItemAddRideWarAttr(user, item, context)
	local ret = _ItemAddRideWarAttr(user,item,context)
	return ret or -1
end

function ItemAddJingJieWish(user, item, context)
	local ret = _ItemAddJingJieWish(user,item,context)
	return ret or -1
end

function ItemAddHorseWish(user, item, context)
	local ret = _ItemAddHorseWish(user,item,context)
	return ret or -1
end

function ItemHorseStar(user, item, context)
	local ret = _ItemHorseStar(user,item,context)
	return ret or -1
end

function ItemAddShenBingHalf(user, item, context)
	local ret = _ItemAddShenBingHalf(user,item,context)
	return ret or -1
end

function ItemAddLingQiHalf(user, item, context)
	local ret = _ItemAddLingQiHalf(user,item,context)
	return ret or -1
end

function ItemAddRideWarWish(user, item, context)
	local ret = _ItemAddRideWarWish(user,item,context)
	return ret or -1
end

function ItemBossHuiZhang(user, item, context)
	local ret = _ItemBossHuiZhang(user,item,context)
	return ret or -1
end

function ItemShenLingSkin(user, item, context)
	local ret = _ItemShenLingSkin(user,item,context)
	return ret or -1
end

function ItemAddShenLingAttr(user, item, context)
	local ret = _ItemAddShenLingAttr(user,item,context)
	return ret or -1
end

function ItemAddCrossScore(user, item, context)
	local ret = _ItemAddCrossScore(user,item,context)
	return ret or -1
end

function ItemAddShenWuRate(user, item, context)
	local ret = _ItemAddShenWuRate(user,item,context)
	return ret or -1
end

function ItemAddYuanLingAttr(user, item, context)
	local ret = _ItemAddYuanLingAttr(user,item,context)
	return ret or -1
end

function ItemAddRealmAttr(user, item, context)
	local ret = _ItemAddRealmAttr(user,item,context)
	return ret or -1
end

function ItemAddYuanLingWish(user, item, context)
	local ret = _ItemAddYuanLingWish(user,item,context)
	return ret or -1
end

function ItemAddShenLingWish(user, item, context)
	local ret = _ItemAddShenLingWish(user,item,context)
	return ret or -1
end

function ItemUseSuoYaoRing(user, item, context)
	local ret = _ItemUseSuoYaoRing(user,item,context)
	return ret or -1
end

function ItemAddQiyuQuest(user, item, context)
	local ret = _ItemAddQiyuQuest(user,item,context)
	return ret or -1
end

function ItemAddXiuWei(user, item, context)
	local ret = _LuaItemAddXiuWei(user,item,context)
	return ret or -1
end

function ItemAddLingQiBaoJi(user, item, context)
	local ret = _ItemAddLiQiBaoJi(user,item,context)
	return ret or -1
end

function ItemAddShenBingBaoJi(user, item, context)
	local ret = _ItemAddShenBingBaoJi(user,item,context)
	return ret or -1
end


function ItemAddJingJieBaoJi(user, item, context)
	local ret = _ItemAddJingJieBaoJi(user,item,context)
	return ret or -1
end

function ItemAddZuoQiBaoJi(user, item, context)
	local ret = _ItemAddZuoQiBaoJi(user,item,context)
	return ret or -1
end

function ItemAddMingYuBaoJi(user, item, context)
	local ret = _ItemAddMingYuBaoJi(user,item,context)
	return ret or -1
end

function ItemAddBaoJiaBaoJi(user, item, context)
	local ret = _ItemAddBaoJiaBaoJi(user,item,context)
	return ret or -1
end

function ItemPackageItemByDay(user, item, context)
     local ret = _ItemPackageItemByDay(user,item,context)
	 return ret or -1
end

--保甲资质丹
function ItemAddBaoJiaZiZhi(user, item, context)
     local ret = _ItemAddBaoJiaZiZhi(user,item,context)
	 return ret or -1
end
--命玉资质丹
function ItemAddMingYuZiZhi(user, item, context)
     local ret = _ItemAddMingYuZiZhi(user,item,context)
	 return ret or -1
end
--神兵资质丹
function ItemAddShenBingZiZhi(user, item, context)
     local ret = _ItemAddShenBingZiZhi(user,item,context)
	 return ret or -1
end
--法宝资质丹
function ItemAddFaBaoZiZhi(user, item, context)
     local ret = _ItemAddFaBaoZiZhi(user,item,context)
	 return ret or -1
end
--境界资质丹
function ItemAddJingJieZiZhi(user, item, context)
     local ret = _ItemAddJingJieZiZhi(user,item,context)
	 return ret or -1
end
--坐骑资质丹
function ItemAddRideZiZhi(user, item, context)
     local ret = _ItemAddRideZiZhi(user,item,context)
	 return ret or -1
end

function ItemAddMingYuAttr(user, item, context)
	local ret = _ItemAddMingYuAttr(user,item,context)
	return ret or -1
end

function ItemAddBaoJiaAttr(user, item, context)
	local ret = _ItemAddBaoJiaAttr(user,item,context)
	return ret or -1
end

function ItemActivateTianShen(user, item, context)
	local ret = _LuaItemActivateTianShen(user,item,context)
	return ret or -1
end
