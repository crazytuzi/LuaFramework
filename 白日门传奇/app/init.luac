--自定义---------
------------------

local scalex = display.width/DESIGN_WIDTH
local scaley = display.height/DESIGN_HEIGHT
local scaleMax = scaley
local scaleMin = scaley
if scalex > scaley then
	scaleMax = scalex
end

if scalex < scaley then
	scaleMin = scalex
end
cc.MAX_SCALE = scaleMax;
cc.MIN_SCALE = scaleMin