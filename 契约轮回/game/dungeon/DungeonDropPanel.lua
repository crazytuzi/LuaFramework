---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by win 10.
--- DateTime: 2018/9/20 11:18
---

DungeonDropPanel = DungeonDropPanel or class("DungeonDropPanel", BaseItem)
local DungeonDropPanel = DungeonDropPanel

function DungeonDropPanel:ctor(parent_node, layer)
    self.abName = "dungeon"
    self.assetName = "DungeonDropPanel"
    self.layer = "UI"
    self.panel_type = 3;
    self.events = {};
    --WorldBossPanel.super.Load(self)
end

function DungeonDropPanel:dctor()
    GlobalEvent:RemoveTabListener(self.events);
end

function DungeonDropPanel:Open()
    WindowPanel.Open(self)
end

function DungeonDropPanel:LoadCallBack()
    self.nodes = {

    }
    self:GetChildren(self.nodes)
    SetLocalPosition(self.transform, 0, 0, 0);

    self:AddEvent();
end

function DungeonDropPanel:AddEvent()
end